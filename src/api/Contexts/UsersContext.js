/*import { create  } from "zustand";

const useUser = create((set) => ({
    user: {},
    addUser: (user) => {
        set((state) => ({user: user}))
    },
}))

const useUser = create((set) => ({
    user: null,
    setUser: (user) => set({ user }),
  }));







export default useUser;*/